version: "3.9"

services:
  furnace:
    build: .
    ports:
      - "8080:8080"        # Map host port 8080 to container port 8080
    volumes:
      - ./configs:/app/configs  # Mount local configs folder
    restart: unless-stopped
